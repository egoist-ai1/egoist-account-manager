# v3.1.5 switch/reauth recovery

- Changed: закрыта подтверждённая production-log гонка `prepare → target reauth → switch rejection`; все pre-write failures теперь переводят подготовленную singleton-транзакцию в terminal state. Duplicate reauth одного профиля заменяет прежний pending login, commit немедленно обновляет active card, а кнопка имеет корректное интерактивное состояние.
- Why: non-terminal prepared transaction блокировала все последующие переключения сообщением `Another switch transaction is already active` до перезапуска Manager; повторные reauth создавали дополнительные 15-минутные blockers.
- Verification (2026-08-02 UTC): typecheck/lint pass; 71/343 Node/integration tests including soak1000; 15 Playwright pass + 4 intentional skips; 78/78 ASAR; packaged startup pass; 44/44 native tray representations; network-disabled Windows Sandbox 3.1.4→3.1.5 lifecycle 16/16.
- Affected contracts: switch transaction terminality, provider/cross-process serialization, reauth replacement, renderer busy lifecycle, release/Sandbox contract. Auth bundle format, DPAPI vault, SQLite schema and Claude Code scope unchanged.
- Durable context: CTX-003 updated to exact local 3.1.5 release-ready state; architecture gains the pre-write terminality invariant. No new candidate.
- Risks: EXE unsigned; real user account switch intentionally not automated; four production-inspector tests remain skipped but package parity/startup passed.
- Next: exact local commit → publication manifest → independent outcome review → guarded push/tag/release only on `pass`.
