# Codex Account Manager 3.1.4

## Чистая прозрачная hover-панель

- Исправлен непрозрачный глобальный `#root`, который оставлял чёрный прямоугольник за скруглённой карточкой.
- Четыре угла 252×144 PNG теперь имеют нулевую alpha; карточка заканчивается ровно на rounded border.
- Удалены внешняя тень, тяжёлый backdrop blur и декоративные glow-ореолы. Содержание — активный профиль, текущий лимит и время сброса — сохранено.

## Читаемый `1%` на панели задач

- Периметральная цветная рамка, визуально похожая на скобку, заменена на короткий нижний state-rail.
- Однозначная цифра получила отдельные поля и упрощённую форму без тяжёлого основания.
- Значения проверены нативно в 16/20/24/32 px; error, stale, unknown и фактический ноль остаются отдельными состояниями.

## Проверенная сборка

- TypeScript, ESLint, 339 Node/integration tests, 15 UI smoke checks, 44 native-image representations, alpha-corner probe, ASAR parity и packaged startup прошли.
- Текущая сборка не подписана Authenticode; SmartScreen может показать предупреждение. Сверяйте SHA-256 с `SHA256SUMS-3.1.4.txt`.
